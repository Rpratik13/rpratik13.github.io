const baseRules = {
  'arrow-body-style': ['error', 'always'], // No shorthand return functions
  'no-console': 'warn', // No console.*
  'no-duplicate-imports': 'error', // No two different import lines from same file
};

export { baseRules };
